#!/usr/bin/env mongo
"""List all data bases in mongoDB"""

db.adminCommand('listDatabases').databases.forEach(function(d) {
    print(d.name + "\t" + (d.sizeOnDisk / (1024 * 1024 * 1024)).toFixed(3) + "GB");
});
